"use client";import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import Image from "next/image";import {supabase} from "@/lib/supabase";
export default function Login(){const router=useRouter();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[msg,setMsg]=useState("");async function submit(e:FormEvent){e.preventDefault();if(!supabase){setMsg("Configure o Supabase para liberar o acesso.");return}const{data,error}=await supabase.auth.signInWithPassword({email,password});if(error){setMsg("E-mail ou senha inválidos.");return}const{data:profile}=await supabase.from("profiles").select("role").eq("id",data.user.id).single();router.push(profile?.role==="admin"?"/admin":"/area-cliente")}
return <main className="auth-page"><section className="auth-brand"><Image src="/logo-has.analytics.png" alt="HAS Analytics" width={250} height={250}/><h1>Seu projeto, documentos e resultados em um só lugar.</h1></section><form className="auth-form" onSubmit={submit}><span className="eyebrow">Área segura</span><h2>Entrar</h2><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="btn primary">Acessar</button>{msg&&<p>{msg}</p>}</form></main>}
